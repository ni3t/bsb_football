<%= form_with(model: league_membership) do |form| %>
  <% if league_membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(league_membership.errors.count, "error") %> prohibited this league_membership from being saved:</h2>

      <ul>
        <% league_membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :league_id, style: "display: block" %>
    <%= form.text_field :league_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
