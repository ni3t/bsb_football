<%= form_with(model: nfl_player) do |form| %>
  <% if nfl_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nfl_player.errors.count, "error") %> prohibited this nfl_player from being saved:</h2>

      <ul>
        <% nfl_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nfl_team_id, style: "display: block" %>
    <%= form.text_field :nfl_team_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
